[Unit]
Description=Remove unused Docker build cache older than 10 days
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/bin/docker builder prune --all --force --filter until=240h
Nice=10
IOSchedulingClass=idle
TimeoutStartSec=30min
